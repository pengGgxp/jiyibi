# 记一笔设计上下文

## Design Context

### Users

- 面向希望随手记录个人收入和消费的单用户。
- 典型场景是付款或收款后立刻打开应用，输入带正负号的金额，并选择补充一句文字或一张截图。
- 核心任务是在几秒内完成记录，同时快速确认当前余额；无需登录，也不需要跨设备同步。

### Brand Personality

- 快、清楚、可信。
- 语言简短、自然、直接，避免会计术语和冗长引导。
- 体验应安静务实，让用户对本地数据和余额计算有掌控感。

### Aesthetic Direction

- 首页是实用工具界面，不做营销式首屏；快速记账控件是第一视觉重点，余额与概览作为紧邻的次级信息。
- 使用克制的中性色作为基础，用语义明确且色盲友好的收入/支出颜色辅助识别，但不只依赖颜色表达正负。
- 布局紧凑但不拥挤，按钮与输入目标适合触控；卡片只用于真正独立的记录项或工具，不把每个页面区块都包装成卡片。
- 同一套响应式结构适配手机、平板与桌面；移动端优先单手操作，宽屏利用空间展示概览和记录，不简单放大字号。
- 主题模式与具体品牌色尚未确定，首版默认采用清晰的浅色主题，并为后续深色主题保留设计令牌。

### Implementation Defaults

- 当前仓库尚无应用代码或既定技术栈。
- 在没有新的明确选择前，采用框架回退方案：Vite + React + TypeScript、Tailwind CSS、Base UI 方向的 shadcn/ui。
- 表单默认使用 TanStack Form；记录列表在需要结构化表格时使用 TanStack Table，数据量大到需要虚拟化时使用 TanStack Virtual。简单 MVP 不为使用库而过度抽象。
- IndexedDB 保存账目、初始余额和本地截图 Blob；localStorage 仅保存轻量偏好设置。
- PWA 使用 Web App Manifest 与 Service Worker，优先静态部署，并保证离线可新增和查看记录。
- 这些实现选择来自新项目的框架回退默认值，不是用户指定；检测到现有栈或用户后续明确选择时，以它们为准。

### Design Principles

1. 首页只设一个明显主动作：直接记一笔；概览要易扫读，但不得与输入争夺注意力。
2. 先让用户保存，再补充细节：金额是唯一必填核心，文字和截图是可选内容，分类不能阻塞记录。
3. 正负必须一眼可辨：同时使用 `+/-`、文字和适度颜色，不让颜色独自承担含义。
4. 余额变化必须可解释：初始余额、每笔变动和当前余额之间保持清晰关系，编辑或删除后立即反馈结果。
5. 多端保持同一心智模型：各尺寸保持相同术语、数据结构和核心操作，只调整布局密度与控件位置。
