# 记一笔设计上下文

## 用户与核心任务

- 面向希望随手记录个人收入和消费的个人用户。
- 典型场景是付款或收款后立即打开应用，输入金额，并补充一句文字或一张截图。
- 核心任务是在几秒内完成记录并确认当前余额；登录和同步是可选增强，不能阻碍本机记账。

## 品牌性格

- 快、清楚、可信。
- 语言简短、自然、直接，避免会计术语和冗长引导。
- 体验安静务实，让用户清楚知道数据已保存在本机、是否已同步，以及冲突是否需要处理。

## 视觉方向

- 首页是工具界面，不做营销式首屏；快速记账是第一视觉重点，余额和概览是紧邻的次级信息。
- 使用克制的中性色为基础，以文字、正负号和色盲友好的收入/支出颜色共同表达语义。
- 布局紧凑但不拥挤，触控目标不小于 44px。卡片只用于独立记录或工具，不把每个页面区块都包装成卡片。
- 手机优先单手操作，宽屏利用空间并排展示记账和概览；所有尺寸保持相同术语与核心操作。
- 云同步状态只占用页头现有状态位；账号、首次合并、错误和冲突处理放在设置中，不与“记一笔”争夺注意力。

## 实现约束

- 技术栈为 Vite、React、TypeScript、Tailwind CSS、Dexie、Vitest 与 Playwright。
- IndexedDB 是即时写入点；网络失败不得阻塞或回滚本机保存。
- 未开启同步时，账目、初始余额和截图仅保存在本机。
- 开启同步后，默认由仅允许指定账号的 GitHub OAuth 负责认证，D1 保存认证哈希、用户映射、账目和同步元数据，Workers KV 保存压缩 JPEG 截图云副本；本方案不依赖 R2 或银行卡。
- 多设备冲突使用服务端版本检测，不以客户端时间戳静默覆盖；冲突时继续显示本机版本并提供明确选择。
- PWA 使用 Web App Manifest 与 Service Worker，静态资源可离线使用，`/api/**` 永不进入离线缓存。

## 设计原则

1. 首页只保留一个明显主动作：直接记一笔。
2. 本机保存与云端同步必须分别反馈，不能把“已保存”误报成“已同步”。
3. 正负同时使用 `+/-`、文字和颜色表达，不能只依赖颜色。
4. 初始余额、每笔变动和当前余额之间的关系必须可解释，编辑和删除后立即重算。
5. 登录前明确说明上传范围；本机待同步数据、冲突和错误必须有恢复路径。
6. 各尺寸维持同一心智模型，只调整布局密度和控件位置。
