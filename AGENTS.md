# AGENTS.md

## 项目概述

- 项目名称：记一笔
- 项目类型：渐进式 Web 应用（PWA）
- 产品目标：帮助个人用最少步骤记录消费和收入，并随时了解当前余额与收支概览。
- 仓库属性：允许作为公开仓库发布；任何提交都不得包含密码、令牌、真实账目或其他隐私数据。

## 产品原则

1. 极速记录：首页直接提供记账入口，优先完成“金额正负号 + 文字或截图”的最短流程。
2. 数据可信：金额、类型、分类和时间必须清晰、可编辑、可追溯。
3. 离线优先：无网络时仍可打开应用、查看已有数据并新增记录。
4. 隐私默认：账目默认保存在本地，未经用户明确同意不得上传。
5. 渐进增强：基础功能在主流现代浏览器中可用，安装能力作为增强体验。

## 初始产品范围

MVP 已确认包含：

- 通过正金额 `+` 记录收入，通过负金额 `-` 记录支出
- 每条记录包含金额，且文字或截图至少提供一项；每笔最多附加一张截图作为凭证
- 截图默认仅在本机保存；用户明确开启云同步后可上传压缩 JPEG 云副本；MVP 不包含 OCR 自动识别
- 设置记录时间，分类与备注作为后续可选增强，不得阻碍极速记录
- 查看、编辑和删除记录
- 按时间展示账目列表
- 当前余额默认从 0 开始；第一条账目、存钱事件或余额调整创建后永久锁定原始初始余额
- 锁定后通过“余额校准”或“更正起点”追加审计记录，不直接覆写初始余额；总余额按“原始初始余额 + 有效收支 + 有效余额调整”计算
- 可选设置长期默认发薪日；发薪日为 29、30、31 而当月无该日期时使用当月最后一天
- 只支持一个当前存钱目标，由截止日期和目标总额组成，不随发薪周期重置，也不提前占用可花余额
- 存入和取用使用独立存钱事件，不伪装成收入、支出或账户转账；历史周期结算只读兼容并继续计入已存金额，新版本不得创建周期结算
- 每次只填写一个预计到账日和一个预计收入；公司临时延期时只调整本次日期，不改变长期默认日；预期不提前计入余额，也不自动沿用到下个周期
- 新建下次收入默认选择严格晚于今天的首个常规发薪日；“改日期”按需展开。“延期本次”保留预期 ID 且不得跨过下一常规发薪日，“设为下次”必须生成新 ID；逾期收入按用户确认的实际到账日入账
- 预计到账日到期后由用户确认实际收入；正金额生成一笔收入账目，0 元只结束本次预期与提醒，不生成零金额账目；上次预计金额只用于下次表单预填
- 首页突出可花余额，同时展示总余额、已存金额、存钱目标进度、“到下次”和“下次收入”的轻量结论，不预测固定支出
- 到账确认、异常交易、存款穿透和退款关联统一进入“待处理”；“稍后”只在当前设备隐藏到当天结束，不进入备份或同步
- 退款或报销可原子关联一笔或多笔原支出；有效分摊冲减原支出的分析金额，编辑、删除与撤销必须联动
- 大额支出确认突出“周期账单 / 仅这一次 / 之后报销”，“按日常算”作为退出操作；账户转账保留在编辑入口，不与三种异常处理并列
- 待报销支出结束前不计个人支出；部分报销后可结束报销并把剩余自付额按日常、周期账单或仅这一次统计，退款与报销到账不计个人收入
- 空账本优先展示快速记账和“设置余额”；第一笔事实后才渐进显示资金摘要、发薪日、预计收入、存钱目标与分析入口
- 顶层使用 `#ledger` / `#analysis` 两个哈希视图；首页保留快速记账和关键资金状态，月度明细、趋势与图表放在分析页
- 消费预测使用最近最多 30 个已完成本地自然日；少于 14 天不下结论，14–29 天标为初步估算，满 30 天标为按近 30 天估算
- PWA 安装及离线访问
- 使用同一套响应式界面覆盖手机、平板和桌面浏览器

在需求明确前，以下内容不属于 MVP：

- 公开注册、多租户管理与复杂账号体系
- 银行卡或支付平台自动同步
- 复杂预算、投资和资产管理
- 完整的会计或企业财务报表

## 技术方向

- 采用适合静态部署的单页 Web 应用架构。
- 使用 Web App Manifest 和 Service Worker 提供 PWA 能力。
- 分析结果、每期需存和图表序列只在客户端从现有账目派生，不持久化、不上传；用户确认的处理方式、确认元数据、恢复分摊、存钱事件与余额调整属于账本事实，按备份 payload v7 和同步协议 v9 持久化。
- 当前总余额、已存金额、可花余额、实际现金流、个人收支与日常花法必须分开计算：存钱不改变总余额且不进入消费速度；普通支出以净自付额进入日常花法；周期账单和一次性支出计入个人支出但不外推；待报销支出结束前不计个人支出，退款/报销不计个人收入；账户转账对全部个人可用资金中性。
- 预测比例使用 `BigInt` 中间值和整数分结果；收入不进入消费速度，未来和软删除记录不进入分析。
- 账目优先存储在 IndexedDB；“待处理稍后”、持久存储申请标记和最近备份时间等当前设备偏好可使用 localStorage，但不得进入备份或云同步。
- 账目文字、金额、初始余额锁、余额调整、存钱事件和截图先写入本机 IndexedDB；用户启用云同步后，账目与同步元数据的云副本写入 D1，压缩 JPEG 截图云副本写入绑定名为 `ATTACHMENTS` 的 Workers KV。
- “多端”首先指跨手机、平板和桌面的响应式体验；登录并明确开启同步后，同一账号可在不同设备间同步数据。
- 用户已批准增加可选的登录与在线同步：本机 IndexedDB 仍是即时写入点，默认生产认证使用只允许指定数字 GitHub User ID 的 GitHub OAuth，Cloudflare Access 与本地身份仅作为兼容路径；D1 保存认证哈希、账目与同步元数据，Workers KV 保存截图云副本，该方案不依赖 R2 或银行卡。
- GitHub OAuth state 必须与 HttpOnly nonce cookie 绑定并在 D1 中只保存哈希；应用会话使用随机令牌和 `HttpOnly`、`Secure`、`SameSite=Lax` cookie，D1 只保存 SHA-256 令牌哈希。
- 账本同步协议 v9 必须继续兼容可无损表示的 v1–v8 默认语义账本：v9 新增周期账单处理方式；实际收入确认必须通过 D1 内部幂等凭据与对应账目原子提交。旧协议无法表达有效余额调整或周期账单时必须返回 `upgrade_required`，不得投影假一致、覆盖新语义或误报同步完成。
- 云同步不得阻塞本机保存；登录前必须明确说明上传范围，多设备冲突不得用客户端时间戳静默覆盖。
- Workers KV 是最终一致存储；刚上传的截图在另一设备上可能短暂不可见，客户端应允许重试且不得影响本机 IndexedDB 保存。
- Workers KV 写入必须持有当前账号代次的短期上传租约；账号删除在活动租约结束前不得报告完成，异常中断的租约需有明确过期机制，并通过 cursor 完整扫描当前账号代次前缀下的 KV key。最后一个租约释放且扫描为空后，必须经过至少 60 秒传播静默窗口并再次全扫描才可完成删除。
- 项目应支持部署到 GitHub Pages、Cloudflare Pages 等静态托管平台。
- GitHub Pages 不提供 Pages Functions、GitHub OAuth 回调、D1 或 Workers KV bindings，因此其部署只支持本机账本，不提供登录与在线同步。
- 技术栈为 Vite、React、TypeScript、Tailwind CSS、Dexie、Recharts、Vitest 与 Playwright。
- 本地开发使用 `npm run dev`，提交前运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build` 与 `npm run test:e2e`。

## 开发约定

- 开始实现前先阅读本文件及仓库内更深层目录中的 `AGENTS.md`。
- 优先沿用现有依赖、代码风格和组件模式，避免无必要的新抽象。
- 每次改动保持范围明确，不顺带重构无关代码。
- 金额计算不得直接依赖浮点数；使用最小货币单位整数或可靠的十进制方案。
- 收支类型与金额符号必须保持一致；持久化层需定义并测试统一的金额规范。
- 删除或编辑记录后必须立即、准确地重算余额与概览。
- 余额调整保存后只允许在 8 秒内软作废；不得提供硬删除或原地编辑，后续纠错必须追加新调整。
- 截图附件需要压缩、大小限制、删除回收和存储空间不足的错误提示。
- 日期时间存储与展示规则必须明确，避免时区导致账目日期变化。
- 所有交互需兼顾键盘操作、屏幕阅读器、触摸目标和颜色对比度。
- 图表必须有可见文字结论、非颜色区分方式和可展开的语义数据表；分析页使用懒加载且必须进入 PWA 预缓存。
- UI 必须覆盖空数据、加载、错误、离线及长文本等状态。
- 不得提交 `.env`、密钥、真实账目、数据库文件或用户导出的隐私数据。

## 质量要求

- 核心账目增删改、金额统计和数据迁移逻辑必须有测试。
- PWA 构建应生成有效的 manifest，并能正确注册 Service Worker。
- 完成界面改动后检查移动端和桌面端布局。
- 提交前运行项目已有的格式化、静态检查、测试和构建命令。
- 若某项检查无法运行，应在交付说明中明确原因和风险。

## 推荐实施顺序

1. 搭建响应式应用骨架、IndexedDB 数据层和 PWA 基础能力。
2. 实现金额正负号、文字/截图附件及极速保存流程。
3. 实现初始余额、当前余额、记录列表、编辑和删除。
4. 实现首页收支概览，再评估分类、筛选和备份导出。
5. 完善离线、无障碍、附件存储、响应式和异常状态体验。

## Git 约定

- 默认分支使用 `main`。
- 提交信息简洁描述意图，例如 `feat: add expense entry form`。
- 不提交生成目录、依赖目录或本地编辑器配置，除非项目明确需要。
- 仓库公开前再次检查提交历史中是否存在隐私或凭据。
