# 隐私说明

“记一笔”采用本地优先的数据方式。

## 未开启云同步

- 账目文字、金额、时间、处理方式、确认状态、检测规则版本、已提示修订、恢复分摊、存钱事件、初始余额、发薪日、存钱目标、下一次预计收入和截图保存在当前浏览器的 IndexedDB 中。
- 应用不会把账目上传到服务器。
- 清除网站数据、卸载浏览器或更换设备可能导致数据丢失，请定期导出加密备份。

## 开启 Cloudflare 同步

- 只有用户在设置中确认开启后，应用才会上传当前账本。
- 默认生产登录使用 GitHub OAuth，并只允许部署者配置的数字 GitHub User ID；应用不接触或保存 GitHub 密码，也不开放公开注册。Cloudflare Access 和本地开发身份仅作为兼容认证路径。
- OAuth 回调校验一次性 state 后建立应用会话。会话 cookie 使用 `HttpOnly`、`Secure`、`SameSite=Lax`，不能被前端 JavaScript 读取；D1 只保存 OAuth state 与会话令牌的哈希、有效期和必要身份关联信息，不保存这些随机值的明文。
- 访问已认证会话时，D1 会创建只含哈希用户 ID、同步状态、同步代次和更新时间的最小闸门；在明确启用同步前，不会创建账目云副本。
- D1 保存账号映射、有效账目、账目处理方式、确认与检测元数据、恢复分摊、存钱事件、初始余额、发薪日、存钱目标、下一次预计收入、实际收入确认金额、同步版本和幂等回执。收入确认回执只用于阻止多设备重复确认，不进入账目列表或备份。账目出现新版本后，旧版本的金额、文字、时间和截图引用会从同步历史中清除，只保留不含账目内容的回执信息。
- 删除同步完成后，D1 不再保留原金额、文字、发生时间或截图引用，只保留记录 ID、版本和删除时间组成的最小墓碑，供长期离线的设备识别删除。
- 绑定名为 `ATTACHMENTS` 的 Workers KV 保存用户选择附加的压缩 JPEG 截图云副本。截图不会公开访问，也不用于 OCR 或内容分析。
- Workers KV 是最终一致存储。截图刚上传后，另一设备可能短暂读取不到并需要重试；本机 IndexedDB 中的保存与查看不受影响。
- 截图被替换或记录被删除后会进入回收队列；上传后尚未被账目引用的截图保留 24 小时重试窗口，并在之后的同步中回收。
- IndexedDB 中仍保留离线副本，以便断网时查看和记账。
- 分析页的日均支出、周期预测、存钱进度、每期建议和图表序列全部在当前设备中由账目与存钱事件实时派生；这些派生结果不写入 IndexedDB、备份、D1 或 Workers KV，也不会单独上传。
- 分析统计只读取已经发生且会影响相应口径的有效账目。普通支出进入日常花法；一次性、可报销和账户转账不用于外推；退款/报销仅通过用户确认的恢复分摊调整原始支出的分析金额。正金额收入仍计入实际余额，但不作为消费速度的负项；下一次收入预期只参与场景比较，不提前计入余额。
- 分析使用最近最多 30 个已完成本地自然日；少于 14 天不会显示“够/不够”结论，14–29 天会明确标注为初步估算。预测不代表保证，也不推断固定支出。
- 退出云端会话不会自动清除当前设备的离线副本；共享设备上应同时清除浏览器网站数据。
- 用户明确确认删除全部云端数据后，服务端会先阻止当前代次的所有设备继续同步，再分批删除 Workers KV 截图；只有对应 KV 删除成功后才清除 D1 附件引用，最后删除账号映射、账目、设置、最小墓碑和同步历史。中断或响应丢失时可使用原代次安全重试。
- 上传在删除过程中迟到时，D1 可暂时保留一条独立清理任务，其中只有哈希用户 ID、同步代次、附件 ID、待删对象 key 和创建时间，不含邮箱、账目文字、金额或截图内容。KV 删除失败时该任务不会提前清除。
- Workers KV 写入期间，D1 还会短暂保存哈希用户 ID、同步代次、随机租约 ID、创建时间和过期时间。删除流程会等待活动租约释放；请求异常中断时租约最长 15 分钟后过期，不包含账目或截图内容。
- 云端删除不会清除任何设备本机 IndexedDB 中的账本或截图。删除完成后，旧设备保存的同步代次失效，其同步、下载和上传请求都会被拒绝；只有再次明确启用云同步后才会创建新的云副本。
- 每次重新开启同步都会绑定一个新的账号代次。同步、截图上传和云端删除都必须携带当前代次；迟到的旧请求不能读取、覆盖或删除新代次的数据。
- 删除时除 D1 已跟踪的截图外，还会使用 KV cursor 完整扫描该账号代次前缀下的 key，包含暂时为空但尚未完成的分页，回收并发上传留下但尚未写入 D1 的对象。最后一个上传租约释放且扫描为空后，还会等待至少 60 秒传播静默窗口并再次扫描，避免最终一致性造成漏删。
- 为防止旧设备在删除后静默重建数据，D1 会继续保留由已验证身份标识派生的哈希用户 ID、`disabled` 状态、单调递增的同步代次和更新时间。该闸门不包含邮箱、账目、截图或原始身份标识。
- 如需连同这个最小化防重建闸门一并移除，Cloudflare 账号所有者需要直接删除对应 D1 状态；移除后，同一身份再次明确启用同步会被视为新账号。

## 备份与密码

- `.jiyibi` 备份在浏览器中通过 PBKDF2-SHA-256 和 AES-256-GCM 加密后下载。
- 备份密码不会保存、上传或提供找回服务。
- 备份文件包含账目、处理方式、确认与检测元数据、恢复分摊、存钱事件、发薪日、存钱目标、下一次收入预期与截图，用户应自行妥善保管。备份内部 payload 当前为 v5；日均支出、周期预测、存钱进度、每期建议和图表序列等派生结果不写入备份。

## 数据安全

- 除 OAuth 登录、回调与登出外，所有云端接口都必须验证同源应用会话；兼容部署也可验证 Cloudflare Access 身份。数据按用户及同步代次隔离，D1 保存认证哈希、账号和账目元数据，Workers KV 只保存压缩 JPEG 截图对象。
- 多设备同时编辑时使用服务端版本检测冲突，不以设备时间静默覆盖。
- 仓库不得提交真实账目、截图、备份、账号、密钥、令牌或 `.env` 文件。

本项目不会接入广告、行为分析、银行卡或支付平台，也不会出售个人数据。当前云截图方案不使用 R2，因此无需为 R2 绑定银行卡。
